import React, { useState } from "react";
import Card from "../Common/Card/Card";
import styles from "./Users.module.css";

const Users = (props) => {
  return <div className={styles["users-wrapper"]}>
    <Card>
      sadasdsa
    </Card>
  </div>;
};

export default Users;
